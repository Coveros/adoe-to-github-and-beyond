// Import the assert library import assert from 'assert'; // Import the getForecast function from the main file import { getForecast } from './main.js'; // Define a test suite for the getForecast function describe('getForecast', function() { // Define a test case for the getForecast function it('should return a list of forecast data for a given zipcode', function() { // Define the zipcode to test
with const zipcode = '90210'; // Call the getForecast function with the test zipcode const forecast = getForecast(zipcode); // Assert that the forecast is an array assert(Array.isArray(forecast)); // Assert that the forecast has at least one item assert(forecast.length > 0); // Assert that each item in the forecast has the expected properties forecast.forEach((day) => {
assert(day.hasOwnProperty('validTime')); assert(day.hasOwnProperty('weatherPrimary')); assert(day.hasOwnProperty('maxTempF')); assert(day.hasOwnProperty('minTempF')); }); }); });
